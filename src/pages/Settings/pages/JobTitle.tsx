import { HamburgerMenu } from "iconsax-reactjs";

const JobTitle = () => {
    return (
        <div>
            <HamburgerMenu />
            JobTitle
        </div>
    );
};

export default JobTitle;
